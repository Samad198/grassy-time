import React, { useState, useEffect } from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
  } from 'react-native';

const Home: React.FC<{
}> = () => {
    
  return (
    <View>
      <Text>Show time when different grass was last cut</Text>
      <Text>Show time when grass needs to be recut</Text>
      </View>
  );
};

export default Home